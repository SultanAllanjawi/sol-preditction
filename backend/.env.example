# Comma-separated list of origins allowed to call this API (your Vercel domain(s))
CORS_ORIGINS=http://localhost:3000,https://your-app.vercel.app

# Minutes between (a) treating a cached ensemble as stale and (b) the background
# retrain sweep of DEFAULT_TRACKED tickers (tickers.py)
TRAIN_TTL_MINUTES=180
